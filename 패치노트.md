# TestMcAlgorithm 패치노트

## 1. 작업 목표

이번 패치의 목표는 세 가지였습니다.

1. `line simulator` 전용 Modbus 서비스(`_modbusGatewayService`)에 `register read async` 기능을 추가한다.
2. `OvrSettingsDialogModel`의 endpoint 11개(OCR 10개 + PM 1개)가 각각 자기 소켓을 가지도록 바꾸고, 각 endpoint가 `0 ~ 100` register block을 비동기로 읽도록 만든다.
3. `MainWindow > IP Settings`에서 `Use`를 체크하고 `Apply`를 누르면, 선택된 endpoint만 실제 connect 되도록 `MainViewModel.ApplyOvrSettingsAsync()`를 수정한다.

추가 조건도 같이 반영했습니다.

- endpoint read는 각자 비동기로 돈다.
- UI 속성 변경은 endpoint마다 중간중간 바꾸지 않고, 한 번에 묶어서 반영한다.
- OCR 전류값이 `BusDiagram`의 OCR 마커와 맞게 연결되도록 이름 매핑도 보강했다.

---

## 2. 수정한 파일

- `Services/IModbusGatewayService.cs`
- `Services/ModbusTcpGatewayService.cs`
- `Models/OvrEndpointSettingsModels.cs`
- `ViewModels/MainViewModel.cs`

---

## 3. 1단계: Line Simulator 서비스에 register read 기능 추가

### 변경 이유

기존 `_modbusGatewayService`는

- `ConnectAsync`
- `DisconnectAsync`
- `WriteSingleCoilAsync`
- `ReadDiscreteInputsAsync`

까지만 지원했습니다.

즉, coil / discrete input 위주였고 register block을 읽는 기능이 없었습니다.

### 변경 내용

`IModbusGatewayService`에 아래 메서드를 추가했습니다.

- `ReadHoldingRegistersAsync(byte unitId, ushort startAddress, ushort numberOfPoints, CancellationToken cancellationToken)`

그리고 `ModbusTcpGatewayService`에서 실제 구현도 추가했습니다.

### 이번 패치에서 실제로 어떻게 쓰는가

`MainViewModel.ConnectAsync()`에서 line simulator connect가 성공하면,

- address `0 ~ 100`
- 총 `101 word`

를 한 번 읽어보고 로그를 남기도록 했습니다.

즉, 지금은 UI에 표를 따로 만들지는 않았지만,

- line simulator service 차원에서 register read 기능이 생겼고
- 실제 연결 직후 한 번 호출되는 상태

까지는 연결해둔 상태입니다.

---

## 4. 2단계: endpoint별 소켓 11개 분리

### 이전 구조

이전에는 `MainViewModel` 안에 아래 필드가 있었습니다.

- `_ovrClients : Dictionary<string, ModbusTcpEndpointClient>`

즉, endpoint 소켓을 ViewModel이 dictionary로 들고 있었고, endpoint 모델은 단순 설정값만 갖고 있었습니다.

### 문제점

이 구조는 다음 단점이 있었습니다.

1. endpoint 자체가 자기 연결 상태를 소유하지 못한다.
2. `Apply`와 polling이 모두 ViewModel의 dictionary에 강하게 묶인다.
3. endpoint 단위 lock / dispose / reconnect 제어가 분산된다.

### 변경 후 구조

`OvrEndpointSettingsModel` 안에 아래를 직접 넣었습니다.

- `Socket : ModbusTcpEndpointClient`
- `IoLock : SemaphoreSlim`

즉, 이제 endpoint 하나가

- 자기 IP/Port/UnitId 설정
- 자기 connect 대상 소켓
- 자기 IO lock

을 같이 들고 있게 됐습니다.

### 추가로 넣은 상태

`OvrEndpointSettingsModel`에 아래 상태도 넣었습니다.

- `RegisterSnapshot`
- `ApplyReadResult(...)`
- `DisposeAsync()`

`RegisterSnapshot`은 이번 패치에서 읽은 `0 ~ 100` register 원본 block을 endpoint별로 저장하기 위한 용도입니다.

---

## 5. 3단계: endpoint 이름과 다이어그램 OCR 마커 이름 매핑

### 발견한 문제

`BusDiagram`의 마커 이름은 다음 형태였습니다.

- `OCR1`
- `OCR2`
- ...
- `OCR10`

그런데 `OvrSettingsDialogModel.Endpoints`의 실제 Name은 다음 형태였습니다.

- `ISEM2-WHRUH_01`
- ...
- `ISEM2-WHRUH_10`
- `GIMAC1000`

이 상태에서는 current dictionary를 endpoint `Name` 기준으로 넘기면, `BusDiagram`의 OCR 마커와 이름이 일치하지 않아 전류값이 표시되지 않습니다.

### 변경 내용

`OvrEndpointSettingsModel`에 `DeviceKey`를 추가했습니다.

예시:

- `ISEM2-WHRUH_01 -> OCR1`
- `ISEM2-WHRUH_02 -> OCR2`
- ...
- `ISEM2-WHRUH_10 -> OCR10`
- `GIMAC1000 -> PM1`

그리고 `MainViewModel`에서 `BusDiagram`으로 current를 넘길 때는 이제 `Name`이 아니라 `DeviceKey` 기준으로 넘기게 바꿨습니다.

이렇게 해서 OCR 10개는 다이어그램 마커와 정확히 연결됩니다.

---

## 6. 4단계: ApplyOvrSettingsAsync() 재구성

### 이전 문제

기존 `ApplyOvrSettingsAsync()`는 endpoint마다 다음처럼 동작했습니다.

- `if (Endpoints[0].IsEnabled) ...`
- `if (Endpoints[1].IsEnabled) ...`
- ...

그리고 더 큰 문제는 endpoint connect에 `_modbusGatewayService`를 쓰고 있었다는 점입니다.

즉,

- line simulator 전용 gateway 서비스
- OCR/PM endpoint 연결

이 서로 섞여 있었습니다.

### 변경 후 흐름

이제 `ApplyOvrSettingsAsync()`는 아래 순서로 동작합니다.

1. 설정 다이얼로그를 닫는다.
2. 기존 OVR polling loop만 멈춘다.
   - 소켓은 바로 끊지 않는다.
3. endpoint 11개 전체에 대해 `ConfigureEndpointAsync(...)`를 `Task.WhenAll(...)`로 병렬 실행한다.
4. 결과 스냅샷들을 UI 스레드에서 한 번에 반영한다.
5. `Use = true`인 endpoint가 하나라도 있으면 OVR polling을 다시 시작한다.
6. 아무 endpoint도 선택되지 않았다면 polling을 시작하지 않고 다이어그램 current도 비운다.

### 핵심 포인트

`Apply` 시점에 실제 connect가 일어나므로, 요청하신

- `Use` 체크
- `Apply`
- 선택한 OCR/PM만 connect

흐름이 맞춰졌습니다.

---

## 7. 5단계: endpoint register read를 병렬 실행하도록 변경

### 요구사항

요구사항은 다음이었습니다.

- 각 endpoint는 비동기로 각자 돈다.
- UI 반영은 한 번에 한다.

### 구현 방식

`RefreshOvrCurrentsAsync()` 내부를 다음 구조로 바꿨습니다.

1. `State.OvrSettings.Endpoints`를 순회하며
2. endpoint별 `ReadEndpointRegistersAsync(...)` task를 만든다.
3. `Task.WhenAll(...)`로 한 번에 기다린다.
4. 모든 endpoint 결과를 모은 뒤 `ApplyOvrSnapshotsAsync(...)`로 UI를 한 번에 갱신한다.

### endpoint가 실제로 읽는 범위

모든 endpoint는 이번 패치에서 고정으로

- start address = `0`
- count = `101`

즉, `0 ~ 100` register block을 읽습니다.

읽는 종류는 endpoint 설정값 `ReadFromInputRegisters`에 따라 달라집니다.

- `true`  -> input register read
- `false` -> holding register read

즉, 주소 범위는 고정 `0 ~ 100`이고,
레지스터 종류만 endpoint 설정에 따라 선택되도록 했습니다.

---

## 8. 6단계: UI 한 번에 반영되도록 만든 이유

이전 구조처럼 endpoint를 하나씩 읽고, 읽을 때마다 바로

- `IsConnected`
- `CurrentValue`
- `StatusText`

를 바꾸면 화면이 endpoint마다 순서대로 깜빡일 수 있습니다.

이번 패치에서는 이를 피하려고,

- background task에서 read만 수행
- 결과는 `EndpointReadSnapshot`에 담음
- 마지막에 `ApplyOvrSnapshotsAsync()`에서 한 번에 반영

구조로 바꿨습니다.

즉, read는 병렬이지만 UI는 배치 처리입니다.

---

## 9. 7단계: CurrentValue 계산 방식

endpoint는 `0 ~ 100` 전체 block을 읽습니다.

그다음 `CurrentRegisterAddress`를 기준으로 현재값을 꺼냅니다.

예를 들어:

- block: `0 ~ 100`
- `CurrentRegisterAddress = 12`

이면 읽어온 block에서 index `12` 값을 꺼내 current로 사용합니다.

그리고 여기에 `CurrentScale`을 곱해서 최종 `CurrentValue`를 만듭니다.

즉,

- raw register는 전체 block으로 읽고
- UI current는 그중 한 register를 골라 scale해서 보여주는 구조

입니다.

---

## 10. 8단계: polling stop / restart 정리

이전 `StopOvrPollingAsync()`는 polling을 멈추면서 client dictionary를 전부 dispose했습니다.

이번에는 Apply 직후에 바로 polling을 다시 올려야 하므로, stop 동작을 두 가지로 나눴습니다.

- `disconnectSockets: false`
  - polling loop만 멈춤
  - endpoint socket은 유지
- `disconnectSockets: true`
  - polling도 멈추고 socket도 끊음

이 구조로 바꿔서,

- Apply 시에는 loop만 재시작
- 앱 종료 시에는 실제 socket 정리

가 가능해졌습니다.

---

## 11. 9단계: Dispose 정리

endpoint가 이제 자기 socket과 lock을 직접 가지므로, 종료 시 정리도 추가했습니다.

`MainViewModel.Dispose()`에서 이제:

1. feedback polling 중지
2. OVR polling 중지
3. `State.OvrSettings.DisposeAsync()` 호출
4. line simulator gateway dispose
5. `_modbusIoLock.Dispose()`

순서로 자원을 정리합니다.

---

## 12. 최종 동작 흐름 요약

### Line Simulator

1. `Connect`
2. `_modbusGatewayService.ConnectAsync(...)`
3. discrete input polling 시작
4. 추가로 holding register `0 ~ 100` 한 번 read해서 로그 출력

### OCR / PM Endpoint

1. `IP Settings`에서 `Use` 체크
2. `Apply`
3. endpoint 11개에 대해 병렬 connect / disconnect 판단
4. 결과 UI 한 번에 반영
5. polling loop 시작
6. polling마다 각 endpoint가 `0 ~ 100` register block read
7. 모든 결과 수집 후 UI 한 번에 반영
8. OCR 전류값은 `BusDiagram` 마커에도 동기화

---

## 13. 이번 패치에서 의도적으로 남긴 점

### 1) Line Simulator register 값 전용 UI는 아직 없음

현재는 line simulator register `0 ~ 100` read 기능과 연결 직후 호출만 넣었습니다.

즉,

- 서비스 기능은 있음
- 실제 호출도 됨
- 하지만 별도 표/차트 UI는 아직 없음

필요하면 다음 단계로

- register grid
- raw word dump
- 특정 address 매핑 뷰

를 붙이면 됩니다.

### 2) PM1은 현재 BusDiagram 마커가 없음

`DeviceKey = PM1`까지는 추가했지만, 현재 `BusDiagram` 쪽에는 PM 전용 marker UI가 없습니다.

즉 PM도 polling / connect / current 계산은 되지만, diagram marker로는 아직 표시되지 않습니다.

원하면 다음 단계에서 PM marker도 `BusDiagram`에 추가하면 됩니다.

---

## 14. 핵심 설계 요약 한 줄

이번 패치는

`설정 모델(endpoint)`이 자기 socket을 가지고,
`MainViewModel`은 endpoint별 read task를 병렬 실행한 뒤,
`UI는 마지막에 한 번에 반영`

하도록 구조를 바꾼 작업입니다.

---

## 15. 추가 패치: OVR/PM endpoint write 함수

이후 추가 요청으로 OVR/PM endpoint에도 write 함수를 넣었습니다.

### 추가된 위치

- `Services/ModbusTcpEndpointClient.cs`
- `ViewModels/MainViewModel.cs`

### 서비스 계층에 추가한 것

`ModbusTcpEndpointClient`에 아래 메서드를 추가했습니다.

- `WriteSingleRegisterAsync(...)`
- `WriteMultipleRegistersAsync(...)`

즉 endpoint 소켓은 이제

- holding/input read
- single register write
- multiple register write

를 모두 처리할 수 있습니다.

### MainViewModel에 추가한 것

`MainViewModel`에는 외부에서 호출하기 쉬운 helper를 추가했습니다.

- `WriteOvrEndpointRegisterAsync(string endpointName, ushort registerAddress, ushort value, CancellationToken cancellationToken = default)`
- `WriteOvrEndpointRegistersAsync(string endpointName, ushort startAddress, IReadOnlyList<ushort> values, CancellationToken cancellationToken = default)`

### endpointName에 들어갈 수 있는 값

이 함수들은 아래 둘 다 받도록 만들었습니다.

- 실제 Name
  예: `ISEM2-WHRUH_01`, `GIMAC1000`
- DeviceKey
  예: `OCR1`, `OCR2`, `PM1`

즉 아래처럼 둘 다 가능합니다.

- `WriteOvrEndpointRegisterAsync("ISEM2-WHRUH_01", 10, 1)`
- `WriteOvrEndpointRegisterAsync("OCR1", 10, 1)`

### write 이후 동작

write만 하고 끝내지 않고, 바로 다시 해당 endpoint를 `0 ~ 100` 범위로 읽어서 snapshot을 새로 만듭니다.

즉 흐름은:

1. endpoint lock 획득
2. 필요 시 connect
3. register write 수행
4. 같은 endpoint를 다시 read
5. 읽은 결과로 UI 상태 갱신
6. log 남김

이렇게 되어 있어서 write 후 UI도 바로 최신값으로 맞춰집니다.

---

## 16. 추가 패치: Use 미선택 장비의 Idle 백그라운드 모니터

이번 단계에서는 `Use = false`인 OVR/PM 장비에 대해, 해당 IP로 ping을 보내서

- 응답이 오면 `Idle`
- 응답이 없으면 `Disable`

로 상태를 자동 표시하는 백그라운드 기능을 추가했습니다.

### 왜 추가했는가

기존에는 endpoint가 `Use = false`일 때 단순히 비활성 취급만 했습니다.

그런데 실제로는

- 장비는 네트워크에 살아 있으나 아직 사용 안 하는 상태
- 장비 자체가 네트워크상에서 응답하지 않는 상태

를 구분하고 싶었기 때문에, `ping` 기반 idle 확인 기능이 필요했습니다.

### 동작 방식

`MainViewModel`에 idle monitor loop를 하나 추가했습니다.

흐름은 다음과 같습니다.

1. 앱 시작 시 `StartIdleMonitor()` 호출
2. 백그라운드에서 주기적으로 disabled endpoint만 수집
3. 각 endpoint에 대해 ping 전송
4. 결과를 모아서 UI 스레드에서 한 번에 적용

즉 구조는

- ping은 백그라운드
- UI 반영은 일괄 처리

입니다.

### Use = true / false 일 때 어떻게 바뀌는가

#### 1) `Use = false`

idle monitor 대상에 포함됩니다.

- ping 성공 -> `Idle`
- ping 실패 -> `Disable`

즉 connect를 하지 않아도, 네트워크상 존재 여부를 계속 확인합니다.

#### 2) `Use = true`

idle monitor가 그 endpoint를 더 이상 검사하지 않습니다.

즉 해당 endpoint는 idle ping 대상에서 빠지고, 이후 상태는

- `Apply` 시 connect 결과
- polling read 결과

로만 관리됩니다.

#### 3) 다시 `Use = false`

다음 idle monitor 주기부터 다시 ping 대상에 포함됩니다.

즉 별도 수동 재시작 없이 자동으로 idle 감시가 재개됩니다.

### 구현 포인트

추가된 핵심 메서드는 다음과 같습니다.

- `StartIdleMonitor()`
- `StopIdleMonitorAsync()`
- `MonitorIdleEndpointsAsync(...)`
- `RefreshIdleStatusesAsync(...)`
- `ProbeIdleStatusAsync(...)`
- `ApplyIdleSnapshotsAsync(...)`

### 상태 반영 방식

idle ping 결과는 모델의 enum 상태를 사용합니다.

- `EndpointStatus.Idle`
- `EndpointStatus.Disable`

그리고 ping 상세 결과는 `Info`에 같이 남깁니다.

예:

- `Ping OK: 192.168.1.10`
- `Ping failed: TimedOut`

### Apply와의 관계

`ApplyOvrSettingsAsync()`에서도 connect 결과를 반영한 직후 `RefreshIdleStatusesAsync(...)`를 한 번 더 호출하게 바꿨습니다.

이렇게 해서

- 체크한 장비는 connect 상태로 전환
- 체크 안 한 장비는 바로 idle/disable 상태 재판정

이 한 번에 정리됩니다.

### 종료 처리

앱 종료 시에는 `Dispose()`에서 `StopIdleMonitorAsync()`를 호출해서 idle monitor loop도 정상 종료되도록 정리했습니다.
